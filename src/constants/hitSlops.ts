import { Insets } from 'react-native'

const hitSlop_8: Insets = { top: 8, bottom: 8, left: 8, right: 8 }
const hitSlop_16: Insets = { top: 16, bottom: 16, left: 16, right: 16 }
const hitSlop_24: Insets = { top: 24, bottom: 24, left: 24, right: 24 }
const hitSlop_40: Insets = { top: 40, bottom: 40, left: 40, right: 40 }

const HIT_SLOPS = { hitSlop_8, hitSlop_16, hitSlop_24, hitSlop_40 }
export default HIT_SLOPS
